from django.apps import AppConfig

class PrayerConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "prayer"  # Asegúrate de que el nombre coincida con el directorio de la aplicación
